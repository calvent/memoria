# 使用 hoisted node_modules 模式，以兼容某些需要扁平化依赖的工具
shamefully-hoist=true

# 自动安装 peer dependencies
auto-install-peers=true

# 启用 strict-peer-dependencies 为 false，避免 peer 依赖冲突导致安装失败
strict-peer-dependencies=false
